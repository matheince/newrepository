1. firebase에서 콘솔로 이동(HowlsLecture) Authentication에서 로그인 방법을 설정
   - 비빌번호 로그인 및 구글로그인 사용설정함(프로젝트 지원 이메일에 자신의 이메일을 적는다)
2. LoginActivity로 이동
   - Auth variable 초기화
     lateinit var auth : FirebaseAuth 로 해결
3. moveMainPage(auth.currentUser) 의 currentUser에 빨간중이 뜨면 밑에 함수안에 FirebaseUser? 물음표 넣어줌으로 해결

               } else {
                   Toast.makeText(this, task.exception?.message, Toast.LENGTH_LONG).show()
               }
           }
       }
       fun moveMainPage(user : FirebaseUser?){
               if(user!=null){
                   startActivity(Intent(this,MainActivity::class.java))
               }
           }

4.
