1.데이터베이스 등록
  데이터 모듈이 필요함.(DTO)
  New -> Package -> model
  - ContentDTO 열어서 class 정의
2. AddPhotoActivity.kt 열어서
   storageRef?.putFile(photoUri!!)?.addOnSuccessListener { taskSnapshot ->
               Toast.makeText(this,getString(R.string.upload_success),Toast.LENGTH_LONG).show()
           }
   위의 taskSnapshot -> 은 addOnSuccessListener 나왔을 때 ctrl + Spacebar 눌러 입력 함.
3. AddPhotoActivity.kt 이동 contentDTO 에 FirebaseStorage에 기록된 내용을 가져 옴.


4. Firebase Store 에서 Add Cloud Firestore to your app 연결 gradle에 라이브러리 추가.
   - 전역변수로 다음 선언
     var firestore : FirebaseFirestore? = null
   - Firebase database 에서 권한부여
       규칙으로 이동 \ref_image\ref_4번 참조
   - Firebase database 정상적으로 올라감
     \ref_image\ref_5번 참조
   - AddPhotoActivity.kt 내에 Howls 과 다른 코드로 6시간이상 헤맸음
     Toast.makeText(this,getString(R.string.upload_success),Toast.LENGTH_LONG).show()
                 // 업로드된 이미지 주소
                 var contentDTO = ContentDTO()
                 var uri = taskSnapshot.uploadSessionUri   <============== downloadUrl 은 없음.