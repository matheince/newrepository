1. activity_main.xml 의 디자인 적용.
   -res 디렉터리에 menu 디렉터리 생성(Android Resource Directory)
   -menu 디렉터리에 bottom_navigation_main.xml 생성 (메뉴디렉터리에 menu Resource File 생성)
   -bottom_navigation_main.xml안에 item 들을 넣어줌.
2. activity_main.xml에 bottom_navigation_main.xml에서 만든 item 들을 볼 수 있음.

    <com.google.android.material.bottomnavigation.BottomNavigationView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            app:menu="@menu/bottom_navigation_main">

    </com.google.android.material.bottomnavigation.BottomNavigationView>
3. 메뉴에 만든 버튼을 MainActivity.kt 에서 인터페이스를 만들어 줘야함.
    class MainActivity : AppCompatActivity(), BottomNavigationView.OnNavigationItemReselectedListener {
        override fun onNavigationItemReselected(p0: MenuItem) {
            TODO("not implemented") //To change body of created functions use File | Settings | File Templates.
        }
    class MainActivity 에 빨간줄이 가면  alt+Enter 를 눌러 Implement Members 한다.
4. activity_main.xml 로 이동
   BottomNavigationView 에 bottom_navigation으로 id 줌

   ***********  println("~~~~~~~~~~~~") run창에서 I/System.out : ~~~~~~~~~~~~ 찍어줌

5. 자동 로그인
  MainActivity.kt 에
  override fun onResume() {
          super.onResume()
          moveMainPage(auth.currentUser)
  }
6. Fragment 넣어줌.
   - com.example.howlslecture 에 New 메뉴로 Kotlin File/Class 로
     AlertFragment.kt,DetailViewFragment.kt,GridFragment.kt,UserFragment.kt 만들고
   - Layout 으로 이동.
     fragment_alarm.xml, fragment_detail.xml,fragment_grid.xml,fragment_user.xml 만들어 줌.

7. 각각의 Fragment 파일에 다음 코드를 넣어 줌
   class  AlertFragment : Fragment(){
       override fun onCreateView(
           inflater: LayoutInflater,
           container: ViewGroup?,
           savedInstanceState: Bundle?
       ): View? {
           return LayoutInflater.from(inflater.context).inflate(R.layout.fragment_alarm,container,false)
       }
   }
8. MainActivity.kt 이동 다음의 코드 완성.
    action_home  ===> detailviewFragment = DetailviewFragment()
    action_search ==> gridFragment = GridFragment()

    action_favorite_alarm ==> alertFragment = AlertFragment()
    action_accout ==> userFragment = UserFragment()
    연결함.

    when(p0.itemId){
                R.id.action_home -> {
                    var detailViewFragment = DetailViewFragment()
                    supportFragmentManager.beginTransaction().replace()
                    return true
                }
    연결 코드 작성후

9. activity_main.xml 이동후 Frame 을 만들어 줘야 함.
   Toolbar 아래에 다음 코드를 넣어 줌
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/main_content">

        </FrameLayout>
10. MainActivity.kt 이동 다음의 코드 완성.
    override fun onNavigationItemSelected(p0: MenuItem): Boolean {

        when(p0.itemId){
            R.id.action_home -> {
                var detailViewFragment = DetailViewFragment()
                supportFragmentManager.beginTransaction().replace(R.id.main_content,detailViewFragment).commit()
                return true
            }
            R.id.action_search -> {
                var gridFragment = GridFragment()
                supportFragmentManager.beginTransaction().replace(R.id.main_content,gridFragment).commit()
                return true
            }
            R.id.action_favorite_alarm -> {
                var alertFragment = AlertFragment()
                supportFragmentManager.beginTransaction().replace(R.id.main_content,alertFragment).commit()
                return true
            }
            R.id.action_account -> {
                var userFragment = UserFragment()
                supportFragmentManager.beginTransaction().replace(R.id.main_content,userFragment).commit()
                return true
            }
            R.id.action_add_photo -> {
                //var detailViewFragment = DetailViewFragment()
                return true
            }

        }
        return false
    }